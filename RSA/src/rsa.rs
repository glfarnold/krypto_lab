pub mod rsa {
    use num_bigint::BigUint;
    use num_traits::{Zero, One};

    pub fn multiply (x: &BigUint, m: &i32, n: &BigUint) -> BigUint {
        let y: BigUint = One::one();
        

        y
    }
}